<div id="settings-container">
    <div class="panel">
        <h2>Settings</h2>

        <p id="notice"><%= notice %></p>

        <%= render 'form', setting: @setting %>
    </div>

    <div class="panel" id="preview">
        <%= render 'preview' %>
    </div>
</div>
